apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dataprotection-pvc
  namespace: savepoint
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  resources:
    requests:
      storage: 1Gi
